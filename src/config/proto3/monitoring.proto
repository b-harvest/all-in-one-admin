syntax = "proto3";

package monitoring;

// 노드 상태 조회 서비스
service Monitoring {
    rpc GetnodeStatus (StatusRequest) returns (StatusResponse);
}

// 요청
message StatusRequest {
    string nodeURI = 1;
}

// 응답
message StatusResponse {
    string status = 1;
}